/**
 * Starter categories, used only the first time the app runs (empty storage).
 * Edit this list to change what a fresh install starts with — new
 * categories can also be created from the UI at any time.
 */
export const DEFAULT_CATEGORIES: string[] = ["Alipay", "Наличные", "Карта"];
